default:
    suites:
        domain:
            filters:  { tags: "~ui" }
            contexts:
                - DomainContext
        web:
            filters:  { tags: "critical,ui" }
            contexts:
                - WebContext
